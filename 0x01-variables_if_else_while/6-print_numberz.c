#include <stdio.h>

/**
 * main - rints all single digit numbers of base 10 starting from 0
 * Return: Always 0 (Success)
 */

int main(void)
{
	int n;

	for (n = 0; a < 10; a++)
		putchar(a);
	putchar('\n');
	return (0);
}
